[project]
name = "nemesis-backend"
version = "1.0.0"
dependencies = [
    "fastapi",
    "pydantic",
    "google-genai",
    "aiohttp",
    "python-multipart"
]

[dependency-groups]
dev = [
    "workers-py"
]
