[project]
name = "nemesis-backend"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "pydantic",
    "httpx",
    "python-multipart"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
