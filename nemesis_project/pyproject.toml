[project]
name = "nemesis-project"
version = "0.1.0"
description = "Lionsgate OmniChain Forensic Engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
]

[tool.workers-py]
# Optional config for workers-py
